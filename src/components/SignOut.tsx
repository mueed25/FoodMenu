'use client';

import { LogoutLink } from '@kinde-oss/kinde-auth-nextjs/components'; // Client-side method for logout

const SignOut = () => (
  <LogoutLink className="">Sign Out</LogoutLink>
);

export default SignOut;
