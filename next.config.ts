import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  images: {
    domains: ['gravatar.com', 'unsplash.com','randomuser.me','localhost', 'lh3.googleusercontent.com','www.google.com', 'images.unsplash.com'],
  },
};

export default nextConfig;
